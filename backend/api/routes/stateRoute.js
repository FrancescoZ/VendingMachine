'use strict';
module.exports = function(app) {
/*     var sensors = require('../controllers/sensorController');
  
    
    app.route('/:userId/sensors')
      .get(sensors.get_sensor_by_user)
      .put(sensors.new_sensor)
      .delete(sensors.delete_sensor);

    app.route('/:userId/sensors/:sensorId')
      .put(sensors.update_sensor); */
    
}